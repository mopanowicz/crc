spring:
  data:
    mongodb:
      uri: mongodb://localhost/test
  datasource:
    url: jdbc:h2:mem:db
    username: sa

spring.mongodb.embedded.version: 4.4.9

logging:
  config: classpath:logging.xml
  kafka:
    properties:
      bootstrap.servers: localhost:19092
      client.id: spring-2-kafka-logback-test
